{
  "devDependencies": {
    "laravel-mix": "^6.0.49"
  },
  "scripts": {
    "dev": "mix",
    "watch": "mix watch",
    "watch-poll": "mix watch -- --watch-options-poll=1000",
    "hot": "mix watch --hot",
    "build": "mix --production"
  },
  "dependencies": {
    "vite": "^4.4.5"
  }
}
